import { Grid } from "semantic-ui-react";

const Register = () => {
  return (
    <Grid columns={12}>
      <Grid.Row>
        <Grid.Column width={12}>Register</Grid.Column>
      </Grid.Row>
    </Grid>
  );
};

export default Register;
