import { Grid } from "semantic-ui-react";

const Login = () => {
  return (
    <Grid columns={12}>
      <Grid.Row>
        <Grid.Column width={12}>Login</Grid.Column>
      </Grid.Row>
    </Grid>
  );
};

export default Login;
