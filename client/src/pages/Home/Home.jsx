import { Grid } from "semantic-ui-react";

const Home = () => {
  return (
    <Grid columns={12}>
      <Grid.Row>
        <Grid.Column width={12}>Home</Grid.Column>
      </Grid.Row>
    </Grid>
  );
};

export default Home;
